{{ $host := printf "common_name=%s.node.hcpoc.consul" (env "HOSTNAME") }}{{ with secret "pki_int/issue/consul" $host "alt_names=server.hcpoc.consul,localhost" "ip_sans=127.0.0.1" "ttl=24h"}}{{ .Data.issuing_ca }}{{ end }}